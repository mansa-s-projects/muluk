Execution mode rules for this task:

1. Do not stop for confirmation on obvious product decisions
2. Do not ask clarifying questions unless blocked by a hard technical dependency
3. If a dependency is missing, create the cleanest fallback or mock structure and continue
4. If copy is weak, rewrite it
5. If UX is flat, improve it
6. If a field should be structured instead of free text, convert it
7. If a CTA is vague, strengthen it
8. If a screen ends in insight only, add an execution step
9. Keep momentum and implement the complete intended flow
10. Narrate decisions briefly and keep shipping

You are building a premium monetization system, not a generic dashboard.